#!/usr/bin/env bash
set -euo pipefail

files=$(git diff HEAD~1 HEAD --name-only --diff-filter=d)

if [[ -z "$files" ]]; then
  echo "No changed files in last commit"
  exit 0
fi

echo "$files" | xargs pnpm prettier --write
